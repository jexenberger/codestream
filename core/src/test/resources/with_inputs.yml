stream: with_inputs
desc: A simple stream with inputs
inputs:
  - hello:
      type: string
      prompt: Enter stuffs
      default: world
      required: true

tasks:
  - echo:
      value: ${hello}